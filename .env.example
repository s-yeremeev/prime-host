# Copy to .env and adjust as needed. All values shown are the defaults used
# when a variable is not set (see playwright.config.ts).

# Target environment
BASE_URL=https://mcp.pananames-dev.com

# Playwright's own default timeout for every action (click/fill/etc.) that
# doesn't specify one itself — may need raising on a slow CI.
ACTION_TIMEOUT_MS=4000

USER=oyeriemieiev@pananames.com
PASSWORD=

